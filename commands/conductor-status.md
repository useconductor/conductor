# /conductor-status

Check the current status of all Conductor integrations.

Use the `conductor_status` MCP tool to get active plugins, configured AI provider, and current user settings.

Then list what each active plugin can do and suggest any plugins the user might want to enable based on the current project context.
